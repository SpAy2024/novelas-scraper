# render.yaml
services:
  - type: web
    name: novelas-scraper
    runtime: node
    repo: https://github.com/SpAy2024/novelas-scraper
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    buildCommand: npm install
    startCommand: node server.js